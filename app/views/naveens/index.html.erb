<p id="notice"><%= notice %></p>

<h1>THE NAME IS </h1>

<table>
  <thead>
    <tr>
      <th>Name1</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <body>
    <% @naveens.each do |naveen| %>
      <tr>
        <td><%= naveen.name1 %></td>

        <td><%= naveen.phone%></td>
        
        <td><%= link_to 'Show', naveen %></td>
        <td><%= link_to 'Edit', edit_naveen_path(naveen) %></td>
        <td><%= link_to 'Destroy', naveen, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </body>
</table>

<br>

<%= link_to 'New Naveen', new_naveen_path %>
